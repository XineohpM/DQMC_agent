# Project test/MCP and local script-adapter runtime environment.
# Tested with Python 3.12. Install with:
#   python -m pip install -r requirements.txt

-c requirements.lock.txt
setuptools
matplotlib
scipy
tqdm
GitPython
-e .[test,mcp]
